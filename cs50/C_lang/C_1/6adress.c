//6
#include <stdio.h>

int main(void)
{
    int n = 50;
    printf("%p\n", &n); //%p prints the address of variable n using & keyword.
    // & : allows to get the address of variable 
    // * : allows to go at specific address
}

/*
    pointers : pointer is just a variable that contains the address of some value.
*/